# `apps/telegram-bot`

Bot de Telegram: botones inline y wizard `/schedule` para programas horarios.

El bot corre **dentro de** [`apps/backend`](../backend/README.md) (grammY, long polling, mismo proceso). No hay código aquí.

Esta carpeta queda reservada por si un día se separa el bot. No duplicar lógica aquí.
